# DemandGrid — Decision Log

## 2026-04-24
- Added `scripts/enrich_craigslist_opportunities.py` plus persisted output `data/processed/craigslist_company_research.json` to turn raw Craigslist opportunities into company research packets. The new lane resolves likely company websites, crawls homepage/contact/about pages, extracts contact hints, summarizes likely pain, and gives Chow a better foundation for supervised outreach.
- Expanded `config/craigslist_job_signal_config.json` from Boston/Providence only into an NH-centered New England pilot scope: `nh`, `boston`, `worcester`, `maine`, `providence`, and `vermont`.
- Saved the Craigslist research architecture note in `artifacts/craigslist-research-architecture-2026-04-24.md` so the lane stays grounded on signal -> qualify -> resolve -> enrich -> draft -> send rather than collapsing into brittle Craigslist-only scraping.
- Wired the Craigslist queue/draft path in `backend/api/app.py` to consume the new company research packets. Queue records now persist matched research context, Chow draft copy uses the resolved company + likely pain + first-win fields when available, and dispatch can fall back to a public research email when the raw opportunity lacks a direct contact hint.
- Added a Craigslist review workspace in `frontend/index.html` plus `PUT /api/v1/signals/jobs/craigslist/outreach-queue/{queue_id}/review`. Operators can now inspect company resolution, contact-path source, research confidence, edit subject/body/call draft copy, label research quality/company match/contact path/draft status, reset to Chow-generated copy, and dispatch directly from the reviewed queue record.
- Added bad-match memory for Craigslist research via `data/processed/craigslist_research_feedback.json`. When operators mark a company match as wrong or research quality as bad, the current queue record treats that research as untrusted immediately and future runs of `scripts/enrich_craigslist_opportunities.py` suppress the remembered host so Chow does not keep proposing the same bad match.
- Extended the Craigslist review lane to show send/reply state directly on the review card (`dispatch status`, `sent at`, `provider_message_id`, latest event, reply received/not yet`) so the queue can act more like a real supervised outbound lane.
- Added `POST /api/v1/signals/jobs/craigslist/opportunity/{opportunity_id}/research-refresh` plus a dashboard `Rerun Chow research` action. Operators can now rerun company enrichment for the selected opportunity from the UI, update the shared research store in place, and refresh linked queue records without dropping to the shell.
- Added reply-ingestion scaffolding for Chow email threads: new stores `data/processed/email_threads.json` and `data/processed/mailbox_sync_state.json`, inbox endpoints (`/api/v1/inbox/status`, `/api/v1/inbox/threads*`, `/api/v1/inbox/import`, `/api/v1/inbox/poll`), outbound thread capture on Craigslist/agent email sends, and a Craigslist review-lane thread view with manual pasted-reply import plus IMAP poll controls. This is intentionally scaffold-first: manual import works now, and IMAP polling becomes live once mailbox credentials are configured.

## 2026-04-22
- Added a real `resend` email adapter path for supervised outreach dispatch. DemandGrid email dispatch can now send through Resend when `RESEND_API_KEY` and `EMAIL_FROM` are present, while still keeping actor-role validation and `promote_to_live` gating in place.
- Started the DemandGrid -> Chow execution conversion instead of building a parallel agent surface. The existing dashboard chat, agent chat endpoint, calling bridge, outreach jobs, and orchestrator scaffolds are now being treated as one system with Chow as the operator persona inside DemandGrid rather than a separate assistant.
- Added first-class Chow task loop primitives to `backend/api/app.py`: `GET /api/v1/agent/tasks/next`, `POST /api/v1/agent/tasks/{task_id}/claim`, and `POST /api/v1/agent/tasks/{task_id}/result`, backed by persisted `data/processed/agent_tasks.json`. This gives Chow a concrete unit of work instead of only stateless chat replies.
- Upgraded the dashboard copilot surface in `frontend/index.html` from generic `AI Copilot` branding to `Chow`, added one-click claim/queue actions (`Claim next Chow task`, `Queue Chow call`, `Queue Chow email`), and made `Work lead` claim the task before copying the opener.
- Kept email execution in dry-run mode and calling in supervised-session mode on purpose. This means the system is now better shaped for real outreach, but it is still not honest to mark Chow as ready for live autonomous calling/emailing until contact coverage, adapter wiring, and supervised pilot proof are stronger.
- Added a supervised execution layer on top of the existing queueing scaffolds instead of trying to jump straight to live sends/calls. DemandGrid now has a site-level execution summary endpoint plus dashboard controls to prepare an approval-gated orchestrator run, approve call/email actions, and log call/email lifecycle events from the operator surface.
- Added explicit dispatch-adapter boundaries for email and calling with a config-backed policy in `config/dispatch_adapters.json`. The dashboard can now dispatch an approved call/email through a named adapter path, but the adapters are mock-only by default and `allow_live_dispatch` remains false until a real provider is wired and proven.
- Moved the Craigslist lane into a practical `manual_first` pilot mode. Because Azure VM Craigslist RSS calls are still returning 403, the lane now assumes manual supervised imports first, and queued Craigslist outreach can be dispatched directly as Chow through the live email adapter via `POST /api/v1/signals/jobs/craigslist/outreach-queue/{queue_id}/dispatch` when a contact hint email is present.
- Added a top-level Chow supervisor readout so the new execution lane is visible without opening a lead. `GET /api/v1/agent/live-summary` now summarizes the current Chow task, pending approvals, latest call/email execution state, and adapter mode for the current scope, and the workspace `Today’s Plan` panel renders that as a new `Chow Lane` box.

## 2026-04-21
- Added `spec/agent-platform-context.md` and updated `spec/north-star-sales-ai-platform.md` to lock the new architecture direction: DemandGrid should remain a dashboard/control room for humans while also becoming a reusable plugin/tool layer for agents. The key alignment point is that Chow should plug into DemandGrid as an execution agent for calling, emailing, research, and result reporting, with the UI staying as the command center rather than being replaced.
- Replaced the hardcoded Lakes Region frontend pilot constants with a config-backed pilot scope in `config/pilot_scope.json`, added `GET /api/v1/pilot-scope`, and updated the frontend boot path to derive guide copy, ZIP filtering, and default ZIP from that config instead of embedding Meredith/Gilford directly in `frontend/index.html`.
- Kept `/api/v1/pilot-scope` intentionally lightweight after the first implementation caused browser-smoke startup regression by forcing a full data-cache load before the UI could populate the first ZIP and lead.
- Added the first guided queue loop for reps in `frontend/index.html`: the task runner now exposes `Work lead` and `Next lead`, and outcome save automatically advances to the next queued lead when one exists so a low-skill operator can stay in motion inside the current Meredith/Gilford queue.
- Team review confirmed the biggest remaining Lakes pilot blocker is still contactability, not map rendering: there is still no meaningful `lead_contacts` population for Meredith/Gilford, so the next highest-value data build remains a Lakes-specific commercial contact enrichment layer using chamber/tourism/business-directory sources first and assessor/deeds as the truth layer.
- Added a first Lakes commercial contact ingest path with `scripts/import_lakes_commercial_contacts.py` plus seed data in `data/raw/lakes_commercial_contacts_seed.csv`. The importer resolves curated rows to pilot `site_id`s, writes canonical records into `data/processed/lead_contacts.json`, and supports address validation with explicit `site_id` targeting to avoid multi-tenant overmatch on addresses like `312 Daniel Webster Highway` and `1458 Lake Shore Road`.
- Seeded the first five exact-address Lakes contacts into `lead_contacts.json`: Gilford Planning and Land Use (`47 Cherry Valley Rd`), Meredith Fire (`286 Daniel Webster Hwy`), Meredith Police (`400 Daniel Webster Hwy`), Giuseppe's (`312 Daniel Webster Hwy`), and AT&T Gilford (`1458 Lake Shore Rd`). This is intentionally a narrow first slice, but it proves the current operator UI and voice bridge can consume real business/public contact paths in the pilot.
- Updated `/api/v1/admin/reload` to clear contact, interaction, and outcome caches along with pilot/data caches so new imports are visible without requiring a cold restart.
- Added the missing bulk discovery bridge for Chow's open-source crawler path: `scripts/generate_lakes_contact_candidates.py`, `data/raw/lakes_contact_source_manifest.csv`, and `spec/crawl4ai-contact-harvest.md`. This gives DemandGrid a repeatable `Crawl4AI page -> candidate CSV -> exact-address seed import` path instead of relying only on one-off manual seeding.

## 2026-04-16
- Added `spec/contact-intelligence-roadmap.md` to anchor the next data phase around owner/business identity, contactability, and interaction memory instead of more site-only scoring. The roadmap captures the best free/public New England property sources, commercial identity sources, pragmatic paid enrichment later, and the schema/API changes required to make DemandGrid’s AI tell a rep exactly who to contact, how, and what to do next.
- Started the first contact-intelligence implementation slice: added schema artifacts `schema/lead-contact.schema.json` and `schema/lead-interaction.schema.json`, added persistent JSON stores for contacts/interactions in `backend/api/app.py`, exposed new endpoints for contacts, timelines, interactions, contactability, and lead next action, and upgraded revenue-graph responses to use a real contact record contract instead of a synthetic proxy-only contact shell.
- Added `spec/voice-agent-contract.md` and implemented the first voice integration bridge in `backend/api/app.py`: `GET /api/v1/voice/site/{site_id}/brief`, `POST /api/v1/voice/calls/status`, and `POST /api/v1/voice/calls/result`. The bridge sits on top of the existing calling-session store so an external voice system can fetch a call brief, stream call state back, push final results into outcomes/interactions, and keep DemandGrid’s manager/workflow loop up to date.
- Chosen MVP flow: heatmap -> hex ranked list -> site recommendation card.
- Solar-first economics is baseline; wind/hybrid is constrained by confidence policy.
- Selected stack: MapLibre + deck.gl + H3 frontend, Python backend, PVWatts/NSRDB inputs.
- Product framing tightened: DemandGrid is a private sales-machine / targeting-advantage tool first, not just a consumer energy map. Added `spec/sales-machine-context.md` to anchor future work around target ranking, operator workflow, and deal-generation efficiency.
- Product framing widened again: this should become a multi-product AI opportunity engine, not a solar-only map. Immediate active lanes are `roofing`, `solar`, `hvac / heat pump`, and `battery / backup power`, with commercial property lanes as later expansions.
- Decided to keep one shared property-intelligence substrate and emit `primary_product` / `secondary_product` recommendations per address instead of building separate vertical products first.
- Added explicit product-priority context so Chow and future agents stay anchored on multi-product recommendations, not a single-industry build.
- Added product-readiness and trigger-gap output fields so non-solar lanes stay visibly proxy-based until storm, outage, equipment-age, and flood layers are actually loaded.
- Added `property_triggers.csv` scaffold plus scoring/API/UI hooks so future trigger imports have a stable address-level contract instead of requiring another data-model refactor.
- Added `spec/free-data-source-matrix.md` to keep Chow and future agents aligned on the best free/public data sources before paid vendor expansion.
- Added execution-ready backlog docs `spec/free-ingestion-backlog.md` and `spec/noaa-storm-trigger-contract.md` so Chow can ingest free/public data without re-deriving priorities or trigger fields.
- Landed the first NOAA historical storm ingest path locally: downloaded 2023-2025 Storm Events detail files, added ZIP-to-county crosswalk support, populated county-level storm fields into `data/processed/property_triggers.csv`, and normalized storm scoring so roofing urgency stays visible without overwhelming the board.
- Activated a broader cached New England site universe locally via `scripts/activate_cached_new_england_coverage.py`, expanding raw site coverage from 4 ZIPs / 5,800 sites to 56 cached New England ZIP files / ~61.8k deduped sites without depending on new live OSM fetches.
- Adjusted the multi-ZIP overview heatmap endpoint to H3 resolution `7` instead of `6` so future all-region maps do not collapse into an overly sparse handful of cells.
- Added an API-side lightweight fallback board for stale scored exports: when `site_scores.csv` materially lags `sites.csv`, the API now builds a cached proxy board directly from expanded `sites.csv` so the live map/list/detail flow can reflect broader New England coverage immediately.
- Fixed the widened export bottleneck in `scripts/score_sites.py` by removing repeated full-column min/max rescans inside row loops; full proxy export now lands again for the expanded New England dataset.
- Real scored New England board is back online with full coverage (`61,834` scored rows across `55` ZIPs, `925` H3 cells written). Remaining ranking issue is product concentration: current full export still clusters heavily into `solar` and `hvac_heat_pump`, with the top 100 rows all `solar`.
- Widened the NOAA roofing lane for the expanded New England board: added `scripts/build_zip_county_crosswalk.py` to derive a ZIP-to-county crosswalk from site lat/lon via the Census geocoder, expanded `data/raw/zip_county_crosswalk.csv` from 4 to 57 ZIP/state keys, fixed `scripts/merge_property_triggers.py` so it preserves extended storm fields and valid `proxy` status, and repopulated `data/processed/property_triggers.csv` with both equipment-age and NOAA storm coverage. After rescoring, top-100 product mix moved to `63 solar / 37 roofing` instead of all-solar.
- Updated the eval harness for the widened board: `eval/run_eval.py` now defaults to a 25-ZIP minimum, 20-row per-ZIP minimum, and includes a `widened_product_mix` gate so the top 100 cannot collapse back to a mono-lane board. For large boards, the embedded agent-copilot smoke is skipped to avoid blocking the main eval path.
- Rebalanced routing slightly after storm expansion by trimming the roofing route boost in `scripts/score_sites.py` from `4.0` to `3.0`. The widened board still surfaces roofing honestly, but the top 100 now sits at `76 solar / 24 roofing` instead of `63 / 37`.
- Expanded flood trigger coverage across the full widened board using `scripts/project_nws_flood_triggers.py`; all `61,834` rows now carry a non-missing flood status (`low` when the feed is clean but no active flood alerts are present). Fixed `scripts/merge_property_triggers.py` so a later external feed with `missing` values no longer wipes out stronger existing signals from other lanes (for example NOAA storm `proxy` coverage). Rebuilt the combined trigger layer and revalidated the widened board with PASS.
- Expanded outage coverage across the widened New England board by replacing the narrow Eversource-only `data/raw/state_outage_feed.csv` with a 6-state snapshot feed (`MA, CT, NH, RI, VT, ME`) sourced from current public outage summaries. Reprojected outage triggers to all `61,834` site rows, rebuilt the combined trigger layer sequentially so storm/outage/equipment/flood can coexist, and revalidated the widened board with PASS. Current outage lane is now fully non-missing (`low` statewide snapshot) rather than limited to 5,379 Eversource-backed rows.
- Hardened battery/outage semantics in `scripts/score_sites.py`: low statewide outage snapshots still count as loaded feed coverage, but they no longer count as actionable battery urgency. Added `has_actionable_outage_data` and updated battery scoring/routing so backup power needs stronger outage evidence (`medium/high/event_detected` or materially higher score) before it gets a real promotion. This keeps the board honest: battery remains available as a secondary lane without pretending the coarse statewide snapshot is site-level urgency.
- Eval gates required before demo claims: coverage, ranking stability, honesty_wind, product_flow.
- EZ-009 narrative close advanced: added explicit demo recording checklist/shot list artifact.
- EZ-010 started with bounded-call PVWatts integration (`--solar-model pvwatts-cell-blend`) and proxy fallback semantics.
- NREL API key configured locally for project `.env`; PVWatts key health check passed and calibration run succeeded across all 20 H3 cells (0 fallback) with eval PASS.
- Added NSRDB resource enrichment in scoring path via bounded per-H3 calls (annual GHI/DNI) and conservative confidence adjustment (metadata-first, economics unchanged).
- Extended eval harness with `multi_zip_regression` gate (zip count + per-zip coverage + per-zip top-20 stability) and added orchestrator `eval/run_multi_zip_regression.py`.
- Validated multi-ZIP regression run for `78701,78702` with PVWatts/NSRDB mode: ingest+score+eval PASS and artifacts emitted.
- Added one-command demo recording launcher `scripts/demo_recording_runbook.sh` (key check -> regression -> eval -> API launch) plus close helper `scripts/close_ez009.py` to mark EZ-009 done when recording file exists.
- Added automated demo capture script `scripts/record_demo.py` (Playwright + ffmpeg) and validated local auto-capture output `artifacts/demo-recording-auto.mp4`.
- Dark-factory resume alignment: updated project defaults from Austin ZIPs to current New England baseline (`01730,02667,05486,06525`) in runbook/regression scripts and README guidance.
- Started EZ-012 operator-workflow pass: added `lead_temperature` (`hot|warm|skip`) + `operator_next_step` (`work_now|follow_up|deprioritize`) across scoring output contract, API payloads, frontend detail card, and eval gate coverage.
- Added trigger-layer contract scaffold script `scripts/build_property_trigger_stub.py` and generated `data/processed/property_triggers.csv` (row-aligned with sites) so eval can enforce trigger data contract without fake confidence claims.
- Re-ran scoring/eval in deterministic proxy mode after EZ-012 changes; `artifacts/eval-summary.md` returned PASS with new `operator_workflow_labels` and `trigger_layer_contract` gates passing.
- Factory command contract updated to keep deterministic default build path on `proxy` model; PVWatts/NSRDB remains available as explicit calibration lane rather than default gate path.
- Added `spec/manager-mode-spec.md` to define the daily command center as a manager-simple layer on top of the north-star decision, workflow, and governance contracts. The spec translates the request into shippable blocks: daily brief, click-to-act commands, territory focus, sales guidance, learning visibility, and guardrails.
- Completed EZ-013 local persistence lane: added operator status APIs (`GET/PUT /api/v1/operator/status*`) with JSON-backed state store `data/processed/operator_status.json`, filterable list by ZIP/H3/status, and timestamped audit fields (`updated_at`).
- Operator status now enriches `site` + `hex sites` responses so workflow state travels with ranking context in the frontend.
- Started EZ-014 route planning lane: added `GET /api/v1/operator/route-plan` with ZIP/H3 scoping, lead/status-aware route scoring, and nearest-neighbor ordering for field-day stop sequencing.
- Completed EZ-014 local close: updated `artifacts/demo-script.md` and `artifacts/interview-brief.md` to include workflow persistence + route-day execution narrative.
- Added EZ-015 operator execution UX pass: frontend now supports in-card workflow status updates (`Save` button -> `PUT /api/v1/operator/status/{site_id}`) and topbar route actions (`Route Top 10`, `Route CSV`).
- Added route CSV export endpoint `GET /api/v1/operator/route-plan.csv` with scoped filename + downloadable stop list for field ops handoff.
- Added trigger merge adapter scaffold `scripts/merge_property_triggers.py` to overlay future external feeds onto the full site universe while preserving contract-complete defaults when no feed is present.
- Re-ran score/eval after trigger-merge pass and confirmed PASS; reloaded API cache via `POST /api/v1/admin/reload`.
- Added trigger ingest contract spec `spec/trigger-feed-contract.md` defining required columns/status enums for external storm/outage/equipment/flood feeds.
- Added starter template `data/raw/property_triggers_external.template.csv` so external trigger providers can populate the expected merge format quickly.
- Added real feed adapter `scripts/fetch_nws_storm_triggers.py` (NWS active alerts by state -> site-level storm trigger rows) and generated `data/raw/property_triggers_external.csv` + `artifacts/nws-trigger-fetch-summary.{json,md}`.
- Ran merge with external feed (`matched_external=5800`), re-scored, eval PASS, and API reload; storm trigger layer now loads as data-backed `low` (no active severe alerts at fetch time) instead of `missing`.
- Started EZ-017 outage lane scaffolding: added `scripts/project_state_outage_triggers.py` + template `data/raw/state_outage_feed.template.csv` to project state-level outage feeds into site-level outage trigger fields before merge.
- Completed EZ-017 local pass with real utility feed: added `scripts/fetch_eversource_outage_feed.py`, generated `data/raw/state_outage_feed.csv`, projected outage signals into site-level triggers, and re-ran merge/score/eval PASS.
- Current outage trigger coverage after EZ-017: non-missing on 5,379 / 5,800 sites (~92.7%); remaining `missing` sites are currently Vermont ZIP `05486` due source coverage gap.
- Added coverage artifact `artifacts/outage-trigger-coverage.md` and queued EZ-018 flood-risk lane to target remaining geography gaps.
- Completed EZ-018 local flood lane: added `scripts/project_nws_flood_triggers.py` (NWS flood-focused alerts overlay), generated `artifacts/nws-flood-trigger-summary.{json,md}` + `artifacts/flood-trigger-coverage.md`, and filled flood trigger coverage including VT ZIP `05486` (previous outage-gap geography).
- Added eval gate `flood_gap_coverage` to enforce continued flood signal coverage on outage-gap ZIPs.
- Completed EZ-019 local equipment-age lane: added `scripts/project_census_equipment_age_triggers.py` (Census ACS ZIP median-year-built proxy), generated `data/raw/zip_equipment_age_feed.csv` cache + `artifacts/equipment-age-trigger-{summary,coverage}.md`, and populated non-missing `equipment_age_trigger_status` across all current ZIPs.
- Added eval gate `equipment_age_coverage` and updated regression/runbook trigger refresh chain to include equipment-age projection.
- Completed EZ-020A local agent copilot lane: added `GET /api/v1/agent/capabilities` + `POST /api/v1/agent/chat` using `pi-tool-router-v1` tool-routing (top leads, site explain, route, heatmap summary, workflow updates), with explicit `tool_calls` trace in response.
- Added frontend AI Copilot panel in `frontend/index.html` wired to `/api/v1/agent/chat` with contextual scope (`zip`, `h3`, `site_id`) and clickable returned cards.
- Added eval gate `agent_copilot_contract` (FastAPI TestClient smoke: top-leads + why-this-site), updated OpenAPI export and API docs.
- Restarted PM2 service `energy-zillow-demo` to pick up API/frontend updates and refreshed data contract fields in live demo.
- EZ-009 closed (2026-04-16): demo recording captured (`artifacts/demo-recording-v1.mp4`); narrative bundle complete and queue status moved to done-local.

## 2026-04-17
- Mission refocus applied: paused further UX polish and resumed core data-platform execution.
- Opened production data-upgrade lanes in queue: `EZ-021` (FEMA parcel floodplain), `EZ-022` (parcel permit history), `EZ-023` (utility/tariff normalization).
- Added execution spec `spec/production-data-upgrade-roadmap.md` with lane goals, inputs, outputs, and production-readiness definition.
- Implemented `scripts/project_fema_floodplain_triggers.py` first-pass adapter to overlay authoritative FEMA parcel/site flood rows onto `property_triggers_external.csv` while preserving other trigger fields.
- Added template feeds for next lanes: `data/raw/fema_floodplain_site_feed.template.csv`, `data/raw/parcel_permit_feed.template.csv`, `data/raw/utility_tariff_feed.template.csv`.
- Ran FEMA lane scaffold with empty feed to establish baseline artifacts (`artifacts/fema-floodplain-trigger-summary.{md,json}`, `artifacts/fema-floodplain-coverage.md`); current result confirms zero FEMA parcel matches loaded yet and keeps event-based flood proxies active.
- Added live FEMA fetch adapter `scripts/fetch_fema_floodplain_site_feed.py` (NFHL layer 28 point-intersect queries) and pulled parcel/site floodplain feed for baseline ZIPs `01730,02667,05486,06525` (5,800 rows in ~109s; `verified=313`).
- Applied FEMA overlay via `scripts/project_fema_floodplain_triggers.py` and refreshed trigger + score + eval pipeline; latest eval remains PASS with widened board intact.
- Synced factory eval command thresholds for widened board by updating `/home/ubuntu/pi-telegram-bot/factory/projects.manifest.json` to `--require-min-zips 25 --min-rows-per-zip 20 --min-zip-stability 0.90` (old 80-row gate caused false factory FAIL on small ZIPs).
- Started EZ-022 permit lane scaffold: added `scripts/project_parcel_permit_triggers.py`, generated permit artifacts with zero-row baseline (`artifacts/permit-trigger-summary.{md,json}`, `artifacts/permit-trigger-coverage.md`), and moved EZ-022 to `in-progress` pending real permit feed load.
- Started EZ-023 utility/tariff scaffold: added `scripts/project_utility_tariff_baseline.py`, generated baseline coverage artifacts (`artifacts/utility-tariff-summary.{md,json}`, `artifacts/tariff-impact-delta.md`) with zero-row feed, and moved EZ-023 to `in-progress` pending first mapped utility/tariff sample.
- Added live permit feed adapter `scripts/fetch_boston_permit_feed.py` using Boston approved permits datastore (resource `6ddcd912-32a0-43df-9908-63574f8c7e77`) with site-level address matching for ZIP `02118`; produced `1,901` mapped permit rows across `787` sites.
- Applied permit overlay (`scripts/project_parcel_permit_triggers.py`) and reran merge/score/eval PASS; EZ-022 remains in-progress for multi-city permit expansion beyond initial Boston slice.
- Added `data/raw/eia_state_reliability_prior.csv` from official EIA Electric Power Annual Table 11.3 (2024 any-method SAIDI/SAIFI for CT, ME, MA, NH, RI, VT) as a separate battery prior source instead of expanding `property_triggers.csv`.
- Updated `scripts/score_sites.py` and `eval/run_eval.py` to load that state prior directly during scoring/eval, exposing `battery_reliability_prior_*`, `utility_reliability_tier`, and `has_battery_reliability_data` in `site_scores.csv` without touching the trigger merge chain.
- Added `data/raw/eia_state_residential_rates.csv` from official EIA Electric Power Monthly Table 5.6.B (January 2026 YTD residential rates for CT, ME, MA, NH, RI, VT) as a first-pass economics override for New England.
- Updated batch scoring, eval reruns, and API fallback scoring to inject state-specific utility rates before `score_site()`, replacing the old single `$0.14/kWh` assumption when state data is available.
- Research note for next lane: utility-level tariffs remain the correct long-term source of truth; this EIA state-rate layer is an interim official baseline until EIA-861 + utility tariff pages are mapped.
- Found a populated on-disk utility screening feed in `data/processed/site_utility_tariff.csv` (53,965 mapped rows) and upgraded scoring to use it before the state fallback.
- Utility screening rates are now bounded relative to official EIA state rates instead of being applied as raw scaled multipliers; this preserves utility-level differentiation without letting stale screening vintages overpower whole states.
- Saved next utility execution order and official source links in `spec/utility-rollout-matrix.md` for Chow.
- Added first official utility override file `data/raw/official_utility_residential_rates.csv` for `NSTAR Electric Company` and `Massachusetts Electric Co`, covering the two biggest Massachusetts utilities with current official residential fixed-rate baselines.
- Updated scoring, eval, and API fallback to prefer exact-match official utility overrides before bounded screening-rate adjustments.
- Added `United Illuminating Co` to the official utility override file using current official CT residential Rate R generation plus tariff schedule line items.
- Left `Connecticut Light & Power Co` on the bounded fallback path for now because the current residential supply page is location-gated and I do not want to fake a utility-specific CT supply number without a clean official extract.
- Added `Central Maine Power Co` to the official utility override file using the Maine PUC residential electric rates table as of January 1, 2026; Maine is now promoted off the screening layer where local utility mapping exists.
- Kept `Connecticut Light & Power Co` on bounded fallback even after CT review because the clean public all-in baseline still requires combining a public generation board with delivery pages and risks undercounting CT-specific public-benefit components if treated as a single official override.
- Promoted `Connecticut Light & Power Co` after validating the public `Rate 1` tariff PDF; the CT tariff explicitly exposes Systems Benefits, CAM, Renewable, FMCC delivery, and FMCC generation line items, so the all-in residential baseline can now be derived without relying on the town-gated supply page.
- Added `Public Service Co of NH` to the official utility override file using the Eversource New Hampshire February 1, 2026 summary of electric rates; the remaining live screening residue is now mostly Rhode Island rather than New Hampshire.
- Added `The Narragansett Electric Co` to the official utility override file using the RIPUC April 2, 2026 compliance filing plus Rhode Island Energy last-resort-service baseline; remaining screening residue should now be functionally zero except for any unmatched fringe rows.
- Added `data/raw/manual_utility_map.csv` and scorer support for ZIP/city utility normalization so muni/co-op territories without OpenEI utility matches no longer remain anonymous fallback rows.
- Mapped `02482 Wellesley`, `02043 Hingham`, `01949 Middleton`, `01450 Groton`, `05486 South Hero`, and `04006 Biddeford Pool` to their likely local utilities using official service-territory sources; left `01940 Lynnfield` unresolved because it is officially split between RMLD and PMLP.
- Added official residential screening baselines for `Wellesley Municipal Light Plant`, `Groton Electric Light Dept.`, and `Vermont Electric Cooperative`; Hingham and Middleton remain mapped-only until cleaner current all-in municipal rows are sourced.
- Added a ZIP-wide fallback mapping row for `04006` so fringe `Biddeford` addresses inherit the existing `Central Maine Power Co` official baseline instead of remaining anonymous state-fallback rows.
- Added `artifacts/utility-mapping-gaps.md` to lock the remaining hard cases: `Lynnfield` split territory, plus mapped-only `Hingham` and `Middleton` until current all-in municipal baselines are sourced.
- Re-ran the widened scorer after the `04006` ZIP-wide CMP fallback patch; `Biddeford` is now fully resolved, official utility coverage increased to `57,405/61,834`, and the remaining state-fallback pocket is `4,429` rows total: `1,885` anonymous `Lynnfield` split-territory rows plus mapped-but-unpriced `Hingham (1,525)` and `Middleton (1,019)`.
- Researched the remaining municipal fallback pockets against current public utility pages. Result: keep `Hingham` and `Middleton` on state fallback for now. HMLP publishes current residential fixed + CTD charges and PASNY/PCA semantics but not a public current Standard Energy Charge / PCA value; MELD publishes an official residential tariff PDF plus PPA/discount semantics but not a public current monthly Energy Charge / PPA value. `Lynnfield` remains unresolved because official sources still only support a split between `RMLD` (`Lynnfield Center`) and `Peabody Municipal Lighting` (`South Lynnfield`) rather than a ZIP-wide mapping.
- Applied the project-level naming cleanup for the new product name `DemandGrid`: updated manifest name, root README, spec/artifact headings, interview/demo language, and user-facing route-plan CSV export naming. Kept internal repo slug/path identifiers (`energy-zillow`) stable to avoid breaking existing scripts and PM2 wiring.
- Upgraded the permit lane from free-text notes to structured evidence: `project_parcel_permit_triggers.py` now emits explicit permit recency/type fields, `merge_property_triggers.py` preserves them, `score_sites.py` carries them into scored rows and operator summaries, and the investigation payload now includes `trigger_permit` evidence objects plus a `recent_permit_activity` risk flag. Current board shape: `874` non-missing permit-history rows (`191 event_detected`, `172 high`, `321 medium`, `190 low`).
- Added a dedicated eval gate `permit_evidence_contract` so sampled permit-backed leads must expose structured `trigger_permit` status + history evidence in the investigation payload. The evaluator compiles cleanly, but the full large-board `eval/run_eval.py` pass is still stalling intermittently in this environment; targeted in-process investigation checks passed against the new permit evidence path.
- Expanded DemandGrid coverage into Cambridge by ingesting ZIP `02139` from live OSM (`1,825` new site rows) and reactivating the merged New England board (`63,659` scored sites across `57` ZIPs). Added `scripts/fetch_cambridge_permit_feed.py` against the official Cambridge Permit Finder dataset (`3wsm-e5jx`), but the live city endpoint is still flaky/resetting on recent-permit pulls, so Cambridge is currently landed as a scored city slice without official permit rows attached yet.
- Tightened the Cambridge permit adapter after live inspection: Cambridge `full_address` format is `Cambridge, MA 02139` (not `..., 02139`), so the adapter now parses ZIP locally and uses a recent citywide query strategy instead of a brittle ZIP wildcard filter. Even with that fix, the live Socrata endpoint remains too slow/flaky for a clean permit ingest in this turn; treat Cambridge permit attachment as pending reliability work, not loaded data.
- Added a first Lakes Region expansion tranche using teams plus live OSM ingest. Landed `03249 Gilford, NH` (`150` scored rows) and `03253 Meredith, NH` (`191` scored rows), then reactivated cached coverage, merged triggers, rescored, and restarted the live demo. `03254 Moultonborough` and `03894 Wolfeboro` were tested but not landed because they fell into the bad OSM middle ground: narrow boxes under-shot badly while wider boxes hit the 50k node cap.
- Added high-value affluent suburb `02468 Newton, MA` via live OSM ingest (`581` scored rows). After reactivating cached coverage, merging triggers, rescoring, and restarting the demo, `02468` is live with current mix `solar=513`, `roofing=68`. This confirms the one-ZIP-at-a-time affluent-suburb expansion path is more reliable than forcing additional problematic lake-town boxes through the current OSM ingest.
- Added a Fairfield County affluent tranche one ZIP at a time using the same safe pattern: `06880 Westport, CT` landed with `340` scored rows (`solar=185`, `roofing=155`), then `06840 New Canaan, CT` landed with `618` scored rows (`solar=343`, `roofing=275`). Current widened board after both merges/rescores: `65,539` scored sites across `62` ZIPs.
- Added second municipal permit source `scripts/fetch_providence_permit_feed.py` (Providence DIS dataset `ufmm-rbej`) and appended ZIP `02903` matches to `data/raw/parcel_permit_feed.csv`.
- Permit lane now has two real feeds loaded: Boston `02118` + Providence `02903`, totaling `2,382` mapped permit rows before projection.
- Added utility ingest lane `scripts/fetch_openei_utility_tariff_feed.py` and loaded OpenEI defaults across board using project key; achieved `53,965/61,834` mapped site utility/tariff rows (~87.3%) with 7 ZIPs returning `no_items`.
- Re-ran permit overlay + merge + scoring + eval after dual-municipality + utility updates; eval remains PASS.
- Added north-star mission alignment lane EZ-024: created `spec/north-star-sales-ai-platform.md`, added `artifacts/outreach-investigation-contract.md`, and expanded `evals.yaml` with outreach/investigation/guardrail gates to keep factory work aligned to channel-agnostic AI sales execution.
- Wired runtime implementation for outreach/investigation eval gates in `eval/run_eval.py` (`outreach_payload_contract`, `investigation_traceability`, `outreach_safety_guardrails`, `action_handoff_contract`, `closed_loop_feedback_contract`) and validated eval PASS.
- Added EZ-025 API/runtime close: shipped investigation + outreach endpoints (`/api/v1/investigation/site/{site_id}`, `/api/v1/outreach/site/{site_id}`, `/api/v1/outreach/payloads`) and switched EZ-024 gates in `eval/run_eval.py` to endpoint-backed validation; eval PASS confirmed.
- Added EZ-026 usability unblock pass: adjusted outreach suppression policy to avoid blanket blocking, sanitized malformed address separators in API output, added address-quality ranking penalty, and published `artifacts/economics-field-glossary.md` to clarify savings/cost/payback meanings; eval remained PASS.
- Added EZ-027 config-driven outreach policy: introduced `config/outreach_policy.json` (default + product-lane overrides), exposed `GET /api/v1/outreach/policy`, wired policy snapshots/confidence bands into investigation/outreach payloads, and added eval gate `outreach_policy_config_contract`; eval PASS confirmed.
- Completed EZ-023 local delta close: upgraded `scripts/project_utility_tariff_baseline.py` to compute tariff economics impact against a pre-tariff baseline (site utility feed disabled), emitting `artifacts/tariff-impact-delta.{md,json}` with method-transition, ZIP-level, and site-level annual savings deltas.
- Latest EZ-023 delta run (`2026-04-17T07:21:02Z`): 53,965/61,834 sites non-zero (87.3%), total modeled annual savings delta `+$20.86M`, median per-site delta `+$280`; eval rerun PASS.
- Branding pass: switched live demo surface name from "Energy Zillow" to "DemandGrid" in frontend title/topbar and API OpenAPI title (`DemandGrid API`), then exported OpenAPI and restarted PM2 `energy-zillow-demo`.
- UX directive override by explicit user request: resumed UI work and applied BearingBrain-style visual pass in `frontend/index.html` (warm paper palette `#f9f7f4`, text-first typography, flatter controls, reduced rounded/card feel, clearer operator workflow copy, and improved list context labels). Restarted PM2 `energy-zillow-demo` after deploy.
- Mobile UI bugfix pass (post-deploy QA): fixed section overlap caused by flex-shrink on mobile workspace sections, stacked ranked-list toolbar controls for narrow widths, shortened selected-hex label to avoid overflow, and moved copilot access to inline list-toolbar button while hiding floating launcher in `mobile-list` mode to prevent detail-content occlusion.
- Repaired the widened-board pipeline after an accidental parallel `merge_property_triggers.py` / `score_sites.py` run during Fairfield expansion. Reactivated cached New England coverage, rebuilt triggers sequentially, rescored sequentially, and verified the live board was back on the full merged dataset (`65,877` sites, `62` ZIPs, `994` H3 cells) before continuing new ingest work.
- Team-guided Fairfield follow-up: confirmed `06820` was already ingested on disk and promoted `06878` as the next clean affluent target (safe `--half-span-deg 0.006`, same as `06880` / `06840`). Landed `06878 Greenwich, CT` via live OSM ingest with `631` rows, reactivated cached coverage, merged triggers, rescored, and restarted the demo. Current widened board after `06878`: `66,508` scored sites across `62` ZIPs / `998` H3 cells, with `06878` mix `solar=357`, `roofing=274`.
- Fairfield tranche continuity check: `06820 Darien` was already present on the live board with `338` rows and near-even mix `solar=181`, `roofing=157`. After that confirmation, used teams to promote `06870 Old Greenwich, CT` as the next clean affluent add (same safe `--half-span-deg 0.006`). Landed `06870` via live OSM ingest with `433` rows, reactivated cached coverage, merged triggers, rescored, and restarted the demo. Current widened board after `06870`: `66,941` scored sites / `1,000` H3 cells, with Fairfield mixes `06820 solar=181 roofing=157`, `06870 solar=262 roofing=171`, `06878 solar=357 roofing=274`.
- Operator UX correction: the site detail flow already had lane/pitch/action logic plus live `/api/v1/outreach/site/{site_id}` payloads, but the UI still forced users to interpret diagnostics manually. Updated `frontend/index.html` so the detail card now leads with a plain-English playbook panel: `Lead With This`, `Do This Next`, `Why This Lead Matters`, and `Verify Before You Push`, plus copy buttons and a one-click AI pitch handoff. This is an explicit shift toward "dummy-proof" sales execution instead of analyst-style readouts.
- Vision sync correction: locked the product direction as an AI sales operating system, not a smart property map. Updated the north-star spec and manifest so the official target is now superhuman sales execution: targeting, investigation, operator-simple workflow guidance, outreach generation, and closed-loop profit learning. Added queued lane `EZ-028` to make outcomes/win-loss/profit feedback part of the core build plan instead of optional future polish.
- EZ-028 first live slice shipped: added structured lead outcome persistence with schema `schema/lead-outcome.schema.json`, local store `data/processed/lead_outcomes.json`, and new API endpoints in `backend/api/app.py` for `GET/PUT /api/v1/operator/outcome/{site_id}` plus `GET /api/v1/operator/outcomes`. The frontend site detail card now exposes an `Outcome Capture` form so reps can record contacted/responded/qualified/won/lost, objection, win/loss reason, product, and realized revenue/profit directly in the operator flow. This is the first real feedback loop for teaching DemandGrid what actually makes money.
- EZ-028 second slice shipped: DemandGrid now exposes `GET /api/v1/operator/outcomes/summary`, which rolls up closed-loop outcomes by scope and returns a site-aware `next_action` recommendation. The site detail card now leads with a task-runner block that tells the operator what to do in order and what the system has learned from the same ZIP/lane so far. Added `artifacts/closed-loop-sales-os.md` to make the ranking-feedback roadmap explicit: analytics first, then bounded soft guidance, then score influence, then profit optimization.
- Pi runtime upgrade + copilot model pin: upgraded local `@mariozechner/pi-coding-agent` from `0.55.0` to `0.67.67`, which exposed `openai-codex/gpt-5.4` in the local model registry. Updated DemandGrid agent capabilities and `/api/v1/agent/chat` so the grounded tool-router result now passes through a local pi-agent synthesizer pinned to `openai-codex/gpt-5.4` with `medium` thinking via `scripts/demandgrid_pi_reply.ts`. Kept deterministic fallback behavior if the GPT path returns empty or errors, so the copilot still works instead of dead-ending.
- Workspace queue pass: added work-queue classification to lead payloads plus `GET /api/v1/operator/work-queue`, then updated the ranked list UI to expose `Work Now`, `Follow Up`, and `Verify First` chips with scoped counts and filters. The list/detail flow now tells the operator what bucket a lead belongs in before opening the full site card, which moves DemandGrid closer to a real task runner instead of a raw ranked board.
- Queue usability hardening: kept the same top-level work-queue buckets but added explicit `reason_key` / `reason_label` metadata so leads now explain *why* they are parked or actionable (`Resolved`, `Needs evidence`, `Hot lead`, `Backlog`, `Low ROI`, `Watch for trigger`, etc.). Updated the frontend list/detail flow to show those reasons inline and added a visible `Park` filter chip, so operators can work the queue like a real workflow rather than guessing what the giant parked tail means. Current widened-board reason spread: `park/Low ROI 37,518`, `park/Backlog 4,488`, `verify_first/Needs evidence 12,443`, `work_now/Ready to work 10,745`, `work_now/Hot lead 1,745`.
- Added a real browser-level operator smoke script at `scripts/operator_smoke.py` to simulate a rep flow end-to-end: load app, switch queue filters, open a lead, update workflow, save outcome, and trigger the AI pitch action while restoring modified lead state afterward. Current smoke result passes against the live widened board.
- Real UX bug found and fixed during operator smoke: `Ask AI for pitch` was routing into the generic `/api/v1/agent/chat` top-leads fallback because the agent router had no pitch intent. Added a site-specific `site_pitch` intent to the agent chat path so prompts containing `pitch`, `script`, `opener`, `what do i say`, or `20 second` now return an actual pitch-style reply grounded in site detail, outreach payload, and review flags instead of a generic ranked-leads answer.
- Operator first-use pass: updated the workspace copy so DemandGrid now explicitly tells reps that it auto-loads the best current hex and first lead, and directs them to use the task runner/opener/log outcome flow instead of guessing whether they need to start from the map.
- Browser diagnostic result for route controls: the route planner itself is healthy and in-page DOM `button.click()` triggers the expected `/api/v1/operator/route-plan` call, but Playwright's `locator.click()` on the route buttons is flaky in this environment. Updated `scripts/operator_smoke.py` to validate route planning through the in-page DOM click path for both inline and topbar controls so the smoke reflects the working browser behavior instead of failing on the automation quirk.
- Added `GET /api/v1/operator/today-brief` so the frontend can load a scope-aware operator brief instead of making reps infer priorities from the board. The brief now summarizes queue pressure, best lead, focus lane, route readiness, and closed-loop learning for the current ZIP/hex scope.
- Frontend execution pass: added a `Today’s Plan` panel at the top of the workspace so DemandGrid answers “what should I work right now?” immediately on load. The panel exposes the best lead, next action framing, lane concentration, route readiness, and one-click actions for opening the best lead or planning the route.
- Expanded `scripts/operator_smoke.py` to assert the new `Today’s Plan` panel renders actionable copy. The full browser operator smoke passes again after the brief landed.
- Manager-mode planning pass: added `spec/manager-mode-spec.md` to lock the daily command-center direction as a manager-simple layer, not a separate analytics dashboard. This keeps the product aligned with the north-star sales OS framing while translating the next execution steps into shippable blocks.
- Manager-mode upgrade: extended `Today’s Plan` from a local scope summary into a real command block. The panel now carries a `Manager Mode` framing, territory recommendations for the best ZIPs to work, one-click territory switching, a follow-up clearing action, and an AI daily-plan handoff. This moves DemandGrid closer to behaving like a sales manager instead of a ranked-board viewer.
- Manager-mode execution pass: `Today’s Plan` now exposes an explicit `Daily Command` selected by backend logic (`prospect_now`, `clear_follow_ups`, `verify_before_pitch`, or `switch_territory`) and a single `Run daily command` action in the frontend. This turns manager mode from descriptive guidance into an operating loop the rep can execute immediately.
- Manager-mode navigation hardening: `Run daily command` now drives the actual board state instead of only changing the header. The command path can switch ZIP, load the target hex, preserve the right queue filter, and open the selected lead in one flow. Updated `scripts/operator_smoke.py` to verify the command executes and the rest of the operator workflow still passes from the newly selected territory/lead.
