[2026-05-01] The primary goal was establishing stable multi-agent group chat between Mr Chow (Lead Agent on Azure) and @Hog_hector_bot (Hector on Dell/WSL). Hector was repeatedly crashing with SIGTERM signals—initially suspected to be from a watchdog/supervisor, later identified as likely caused by wsl-pro-service (a root-level process), though the user later confirmed Hector was working intermittently. The root issue of why Hector ignores Mr Chow in group chat was identified as an overly restrictive bot-to-bot filter in src/bot.ts that blocked messages from other bots unless they contained explicit @mentions. The filter was modified to allow Chow (identified by user ID 8305218869 or username containing "chow"/"jungle_catbot") to tag Hector. A sed command to fix this introduced a syntax error (duplicate/malformed if statements) that crashed the bot on startup. The fix was applied via Python script to replace the corrupted line with `if (fromAnyBot && !explicitlyCalled && !isChowBotPeer) {`. The model was switched from openai-codex/gpt-5.5 to glm-5.1 in Hector's .env. ALLOWED_CHATS=1340648617 was added to the .env. The bot code was cloned from Mr Chow's Azure instance to the Dell replacing the old buggy version. Key Dell paths: /home/adam/hector-telegram-bot (bot repo), /home/adam/.nvm/versions/node/v24.15.0/bin/npm (Node path). Hector's systemd service is at ~/.config/systemd/user/hector.service. The agent-node-supervisor was disabled. Current status: The bot code fix (bot-to-bot filter + syntax error) has been written to the Dell but the bot is NOT currently running—the user needs to restart it from WSL since SSH-launched processes keep getting killed. The CHOW_PI_PROVIDER=ollama and CHOW_PI_MODEL=glm-5.1 are set in the .env but haven't been tested yet since the bot hasn't stayed up long enough with the new code.
