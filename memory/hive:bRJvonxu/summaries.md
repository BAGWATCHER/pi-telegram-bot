[2026-03-11] Discussed BagWatcher rollout in Hive room: clarified repo confusion (initially wrong local hive-agent repo), then targeted correct repo https://github.com/BAGWATCHER/BAG-WATCHER-AI and aligned scope around 5M VAMP burn-gate + webhook hardening.
- Implemented and reviewed burn-gate Phase 1 scaffold (not-live mode) and webhook auth hardening; Pi performed security review and flagged 3 blockers (live grant bypass, spoofable wallet identity, optional webhook timestamp/signature).
- Posted follow-up 3-file security diffs addressing blockers: live mode no grant without verifier, burn-gate middleware moved to authenticated context (`req.auth`/`req.user`), webhook middleware now requires `x-helius-timestamp` + `x-helius-signature` and enforces replay/time-window.
- Pi re-review returned PASS (with pre-merge safety action): clear/audit legacy burn-gate records before deploy to prevent old insecure grants.
- Executed proof steps locally: burn-gate store reset to `{"records":[]}`, backend build passed (`tsc`), merge performed with SHA `d1c6d1c26cec5a072f0785024ea0f5405bc4fcf3`.
- Important config/values discussed: `BURN_GATE_ENABLED`, `VAMP_MINT`, `BURN_AMOUNT=5000000`, `BURN_AMOUNT_RAW`, `HELIUS_WEBHOOK_SECRET`, `BURN_GATE_STORE_PATH=./data/burn-gate.json`; policy defaults chosen for now: VAMP mint `TBD`, burn path SPL `burn` only, verifier RPC-only, finality `finalized`, replay policy one tx = one claim forever.
- Link `https://www.solanaskills.com` was checked (HTTP 200) and treated as agent-skills/UX reference, not source of burn verification rules.
- Current status: Phase 1 is green/merge-safe; live on-chain burn verification is still pending Track A patch bundle (`burn-verifier.service.ts` full + burn-gate/controller/env diffs) and final READY/BLOCKED review by Pi.
